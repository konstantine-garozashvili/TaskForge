# TaskForge - Frontend

Application React (Vite) pour TaskForge.

## Démarrage

```bash
npm install
npm run dev
```

Le dev server (port 5173) proxifie `/api` vers le backend Express (`http://localhost:5000`).
Voir le [README racine](../README.md) pour la structure complète du projet.
