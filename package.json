{
  "name": "taskforge",
  "version": "1.0.0",
  "private": true,
  "description": "TaskForge - Application de gestion de tickets d'incidents helpdesk (MVP hackathon ForgeWorks)",
  "scripts": {
    "prepare": "husky",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "lint:fix": "npm --prefix backend run lint:fix && npm --prefix frontend run lint:fix",
    "format": "npm --prefix backend run format && npm --prefix frontend run format",
    "format:check": "npm --prefix backend run format:check && npm --prefix frontend run format:check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  },
  "lint-staged": {
    "backend/**/*.js": [
      "npm --prefix backend exec -- eslint --fix",
      "npm --prefix backend exec -- prettier --write"
    ],
    "frontend/**/*.{js,jsx,css}": [
      "npm --prefix frontend exec -- eslint --fix",
      "npm --prefix frontend exec -- prettier --write"
    ],
    "*.md": [
      "npm --prefix backend exec -- prettier --write"
    ]
  }
}
